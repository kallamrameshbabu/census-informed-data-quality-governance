# Non-redistributable data — see README "Data" section
data/raw/online_retail_II.csv
data/raw/online_retail_II.xlsx

# Large derived artifacts regenerated by the notebooks — keep the repo light.
# Comment these out if you want to commit them for full self-verification
# (they are not large for the two included corpora, but the retail-derived
# corrupted copies can be sizable).
data/corrupted/*online_retail*

# Standard noise
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
*.swp
